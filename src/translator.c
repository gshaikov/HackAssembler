#include <stdio.h>

int translate(FILE *binfile, FILE *asmfile)
{
    // create DS for binary search
    // populate with pre-defined symbols
    // walk asm file, populate DS with labels
    // walk asm file, translate to new file, populate DS with @variables and @labels
    return 0;
}
